set( PACKAGE_VERSION @RAILOSTOOLS_VERSION@ )

if( "${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@RAILOSTOOLS_VERSION_MAJOR@" )
    if ( "${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@RAILOSTOOLS_VERSION_MINOR@" )
        set( PACKAGE_VERSION_EXACT TRUE )
    elseif( "${PACKAGE_FIND_VERSION_MINOR}" LESS "@RAILOSTOOLS_VERSION_MINOR@" )
        set( PACKAGE_VERSION_COMPATIBLE TRUE )
    else()
        set( PACKAGE_VERSION_UNSUITABLE TRUE )
    endif()
else()
    set( PACKAGE_VERSION_UNSUITABLE TRUE )
endif()
